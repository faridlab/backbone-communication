//! Thread seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial Thread data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// THREAD SEEDER
// ============================================================================

/// Seeder for thread
pub struct SeedThreadSeeder;

impl SeedThreadSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedThreadSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedThreadSeeder {
    fn name(&self) -> &'static str {
        "SeedThreadSeeder"
    }

    fn order(&self) -> i32 {
        2
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if communication.threads table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM communication.threads")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/thread_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM communication.threads")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

